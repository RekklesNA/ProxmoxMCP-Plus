# Independent review fixes

## Implemented

- Named-target job stores now use target-specific SQLite paths. `JobStore` also filters list queries and validates the persisted target metadata on every direct record lookup, so cross-target get/poll/cancel/retry operations resolve as unknown jobs.
- Named target command policies are instantiated and selected at invocation time for high-risk operations and retries; target toolsets receive the matching policy.
- Container command tools are registered when any configured target has SSH, including configurations with no legacy global SSH section. Invocation routes through the selected target toolset and its SSH configuration.
- Target `verify_ssl` is a strict boolean. Named-target API tunnel local host/port collisions are rejected during configuration validation.
- Job retry specs, metadata, results, audit events, last errors, and URL credentials/query secrets are sanitized before persistence and serialization. Base tool errors also redact URL credentials/query secrets.
- Added regression coverage for cross-target jobs, secret redaction, strict TLS values, and tunnel endpoint collisions.
- Corrected the remaining dispatch bug: multi-target `VMTools` and `ContainerTools` instances now receive `self.target_command_policies[name]`, not the shared legacy `self.command_policy`. Legacy single-target construction still reuses the legacy tool instances and policy.
- Expanded cross-target job regression coverage to assert that `get_job`, `poll_job`, `cancel_job`, and `retry_job` all reject a job ID owned by another target; `list_jobs` remains empty for the other target.

## Verification

- A focused regression test failed before the routing fix with distinct policy objects, confirming the bug, then passed after the fix.
- `./.venv/bin/python -m pytest -q`: **269 passed, 1 skipped**
- Focused target/policy/job suite: **41 passed**
- `./.venv/bin/ruff check src tests`: passed with no output
- `./.venv/bin/python -m compileall -q src tests`: passed
- `git diff --check`: passed
- Reviewed the strict TLS and tunnel-collision changes in `config/models.py`, `core/targets.py`, `core/proxmox.py`, and the existing regression tests; no regression was found within this scope.

## Remaining concerns

- The documented medium-level target-aware discovery/result annotation contract was not expanded beyond existing credential-free target discovery and target-specific routing.
- Persisted retry recipes are sanitized by design; a retry that requires a secret may not be restart-resumable from the persisted recipe alone, but no credential is written to disk or returned to callers.
- Wheel build was not rerun; the prior report records that it is blocked in this environment because `hatchling.build` is not installed.

## High-finding fixes (2026-08-30)

- Sanitized `JobAuditEvent.details` in `as_dict()`, preserving the existing `timestamp`/`event`/`details` structure while ensuring immediate `register_task` responses cannot expose credential values. Persisted audit JSON continues through the same sanitized representation.
- Replaced target metadata `LIKE` filtering with SQLite JSON exact equality via `json_extract(metadata_json, '$.target') = ?`, so `%` and `_` in target names are treated literally.
- Added regressions for secrets in the initial `register_task` audit event and `%`/`_` wildcard characters in target names.

## High-finding verification

- Focused regressions: **2 passed**.
- `./.venv/bin/python -m pytest -q`: **271 passed, 1 skipped**.
- `./.venv/bin/ruff check src tests`: passed with no output.
- `./.venv/bin/python -m compileall -q src tests`: passed.
- `git diff --check`: passed.
